# 发布验证记录 · 2026-09-15

本记录针对招聘展示版的独立源码副本，不使用本机真实客户数据库、渠道凭证或客户图片。

## 后端

环境：macOS ARM64、Python 3.12.14；从 `pyproject.toml` 新建虚拟环境安装。

- 依赖安装成功，`pip check` 通过。
- 完整 `pytest backend/tests`：**688 通过、1 跳过、5 失败**。
- 五项失败都发生在历史迁移测试的最终版本号断言：期待 `20260907_0045`，实际迁移到 `20260908_0046`。这不等于全套测试通过，后续应同步维护测试预期并重新验证。
- 为避免 editable 安装下仓库脚本与 `backend/scripts` 同名冲突，补充 `scripts/__init__.py`；没有改变业务处理逻辑。

失败用例所在文件：

- `backend/tests/test_business_analysis_persistence.py`
- `backend/tests/test_business_recommendation_feedback.py`
- `backend/tests/test_customer_item_migration.py`
- `backend/tests/test_prediction_engine.py`
- `backend/tests/test_traffic_growth.py`

## 前端

环境：Node.js 24.19.0，使用与当前 `pnpm-lock.yaml` 匹配的已有前端依赖。尝试全新 pnpm 10 安装时，最后一个 esbuild 二进制包下载反复断线，因此未将全新前端安装计为通过。

- `npm run typecheck` 通过。
- `npm run build` 通过，并生成 Sites 所需文件；仍有大于 500 kB 的 bundle 提示。
- 经营记录历史、页面控件、客户控件、信息架构、消息时间线、时区及 Sites 的 45 项回归全部通过。
- README 中隔离演示服务器能够启动，页面、脚本、样式和品牌图片请求均为 HTTP 200；本次未重复进行完整浏览器视觉验收。

## 证据范围

合成数据截图仅展示当前界面；不代表真实客户验收。后端自动化结果不代表第三方账号登录、远程渠道、付费模型或生产部署完成。

本次公开树不包含本地数据库、原始客户图片、凭证、内部验收截图或本机运行日志。此记录不宣称对仓库既有公开历史进行了独立安全审计。

---

## 本地优化复验 · 2026-09-16

本节为后续本地修改的验证，不覆盖上方 9 月 15 日发布记录，也不代表 GitHub 已更新。

- `npm run check` 通过：TypeScript、生产构建、108 项交互回归、13 项收支与排序测试、4 项 Sites 测试；合计 125 项前端测试通过。
- 五处迁移测试的最新版本预期更新为 `20260908_0046`，原有数据、表结构与外键检查保留。
- 收支回归实际执行共享计算模块，验证两类退款、待付款不计收入、无付款计划仍显示待回款；品牌测试匹配当前正式图标。
- 新增 `npm run demo`，隔离演示主页与脚本 HTTP 200。README 首页复用 9 月 16 日已检查的 1440×900 合成截图；本轮未改页面布局，也未重复声称浏览器视觉验收。
- 新增前端 GitHub Actions 配置，远端尚未运行。依赖沿用本地已有安装，未把全新依赖安装记为通过。
- 构建仍存在大包提示；未升级依赖或顺带修改无关警告。
- 后端全套复验：**696 通过、1 跳过**（105.22 秒）。在临时源码副本运行，未复制 `.env`、业务数据库、客户图片或日志，使用空 HOME 和现有 Python 3.12 虚拟环境。输出包含 621 条警告，未将其描述为无警告通过。
- 前一次客户流程专用沙箱检查为 686 通过、1 跳过、10 失败；失败均涉及被沙箱拦截的假 Codex 可执行文件或临时 Git 子进程。改用独立源码与空用户目录复验后全部通过，没有删掉失败用例或改业务实现绕过检查。
- 演示检查结束后已停止本轮 `18880` 临时服务；没有重启或修改当前 `8877` 服务。本轮未执行 Git 提交、推送或部署。

---

## 模块化重构复验 · 2026-09-16

- 前端统一检查：类型、构建及 **128 项测试通过**（108 项交互、16 项工作台/收支行为、4 项 Sites）。仍有原有大包提示。
- 新增领域测试覆盖：固定查询次数、分页、本机权限、同时间消息顺序、验收版本与修订冲突、幂等与事件、离线备份恢复与符号链接拒绝、AI 日限额和用量元数据。
- 浏览器使用同一 Ego Lite 任务空间与合成夹具：1440px 桌面和 390px 窄屏无横向溢出；验证逾期筛选、更新时间排序、项目跳转、验收确认前禁用、设置诊断、分页与请求失败恢复。
- 首页实际观察到一次 `/api/workbench/actions` 请求，没有逐客户需求/提案请求。分页夹具验证 102 条客户事项；明确提示当前加载范围。该证据不是大规模生产性能基准。
- 本轮相关交互捕获的运行异常、控制台 warn/error 均为空；浏览器业务写请求为空。真实验收写入只在临时数据库的 API/事务测试中验证。
- `8877` 原 LaunchAgent 受控重启后运行正常；health/readiness/workbench/analysis-usage 均返回 200，实际迁移版本仍为 `20260908_0046`。没有创建第二个后台实例。
- 截图位于 `design-qa-evidence/2026-09-16-refactor/`，全部使用合成数据。
- 最终完整后端隔离复验：**708 通过、1 跳过**，115.43 秒；630 条既有/依赖警告保留记录。源码副本无 `.env`、真实数据库、图片或日志，空用户目录，模型和渠道使用测试替身。新增用例已包含在本次完整结果中。

## 2026-09-16 接单交付迭代：业务关联图

新增只读项目业务关联图，合并重复编辑入口，修复窄上下文指标布局，并区分正式需求读取失败与未关联。`npm run check` 共 131 项前端检查通过（108+19+4），类型检查和构建通过。ego lite 合成页面覆盖 1440/390、实际节点路由、需求读取失败后重试、键盘展开收起、无横向溢出；无业务写请求。8877 健康接口 200，实际服务 JS 与本轮构建 SHA256 一致。没有重新运行后端套件、没有数据库修改、没有提交/推送。范围与截图见 `docs/project-relations-20260916.md`。
控制台补充：合成预览的 `/events` WebSocket 被隔离 CSP 阻止，另有开发/扩展信息提示；不宣称控制台完全无错误或真实通道通过。临时预览已停止。

## 2026-09-16 ERP 参考迭代：回款跟进

参考本地 ERP 实习项目实际源码，实现当前接单余额账龄分组与明细下钻，替换重复侧栏提醒；项目回款区可展开具体收款节点。类型检查、构建及 136 项前端检查通过（108+24+4）。新增 5 项领域测试覆盖日期边界、计划冲突、退款结算、终止项目和金额守恒。ego lite 合成资料验证桌面/手机、分组/搜索/分批加载、历史月份独立、键盘和双收款节点定位；未发起业务写请求，未发现控制台错误，页面无横向溢出。8877 服务资源与本轮构建 SHA256 一致，健康接口 200；未提交/推送，未改后端/数据库，未重复后端套件。详细事实与边界见 `docs/erp-reference-upgrade-20260916.md`。


## 2026-09-16 UI 设计重构

设计变量与 studio-workspace 主题替换八月主题入口，保留业务组件与路由。类型检查、生产构建、136 项回归测试通过。桌面与手机合成数据页面验收、客户列表详情切换、搜索 Escape 及焦点返回已验证。详见 docs/ui-design-20260916.md 和 design-qa-evidence/2026-09-16-ui/。无真实业务写入和 Git 发布。


## 公开更新复验 · 2026-09-17

发布副本从远端 main 创建，纳入工作台架构、项目关联图、回款跟进与 UI 重构。README 和四张公开截图已同步更新，截图只含合成资料。

- 发布副本 npm run check 通过：类型检查、生产构建、136 项前端回归检查。复用本机已有依赖，本项不等同于全新依赖安装验收。
- 排除本地数据库、凭证、客户原图、运行日志、内部 AGENTS.md、参考原图和内部验收目录；本机敏感配置值与明显密钥格式扫描未发现匹配。该扫描不是第三方安全审计。
- 提供 GitHub Actions 前后端检查；远端运行结果以仓库 Actions 为准。
- 保留构建大包提示及既有样式迁移边界，未进行真实渠道发送或业务数据变更。
- 发布副本完整后端复验：708 通过、1 跳过、630 条警告，179.05 秒；空用户目录、无真实环境文件，测试使用隔离数据库及服务替身。
